<div class="photo">
  <% blogpost.uploads.each do |upload| %>
  <%= link_to image_tag(upload.image.url(:thumb)), upload.image.url(:medium), :rel => "shadowbox[#{blogpost.id}]", :src => upload.image.url(:thumb), :title => upload.image_file_name %>
  <% end %>
</div>
